import random
            
def computer_guess(x):
    low = 1
    high = x
    feedback = ''
    while feedback != 'y':
        if low != high:
            guess = random.randint(low, high)
        else:
            guess = low
